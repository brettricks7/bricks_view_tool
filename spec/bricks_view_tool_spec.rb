require "spec_helper"

describe BricksViewTool do
  it "has a version number" do
    expect(BricksViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
